# Task List

- [ ] Initialize Next.js project `leadership-hub` without Tailwind (using Vanilla CSS).
- [ ] Create `docs/` folder inside the project.
- [ ] Copy or recreate the blueprint artifacts (`project_blueprint.md`, `data_model.md`, `module_map.md`, `permissions_model.md`, `technical_architecture.md`) into `docs/`.
- [ ] Initialize Git repository (if not already done by `create-next-app`).
- [ ] Notify user to review the setup.
